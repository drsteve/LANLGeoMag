language: c

compiler: gcc

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libperl-dev libgsl0-dev libhdf5-serial-dev

script:
  - autoreconf -i
  - ./configure
  - make check TESTS="check_Sgp4 check_libLanlGeoMag check_PolyRoots check_McIlwain_L check_DE421"
